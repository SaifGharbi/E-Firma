{% extends 'base.html.twig' %}

{% block title %}Edit CultureParcelle{% endblock %}

{% block body %}
    <section class="container my-5">
        <h1 class="text-center">Modifier la Culture Parcelle</h1>
        
        <div class="text-center mt-4">
    {{ include('culture_parcelle/_form.html.twig', {'button_label': 'Mettre A Jour'}) }}
        </div>


    <a href="{{ path('app_culture_parcelle_index') }}"class="btn btn-secondary">Retour à la liste</a>
        <div class="text-center mt-4">
            {{ include('culture_parcelle/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
