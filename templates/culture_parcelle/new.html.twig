{% extends 'base.html.twig' %}

{% block title %}Ajouter une culture à la parcelle : {% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-lg p-4">
                    <h2 class="text-center text-success">Créer une Nouvelle Culture</h2>
                    {% include 'culture_parcelle/_form.html.twig' %}
                </div>
            </div>
        </div>
    </div>

    <div class="text-center mt-4">
            <a href="{{ path('app_culture_parcelle_index') }}" class="btn btn-primary btn-lg">Retour à la liste</a>
        </div>
{% endblock %}
