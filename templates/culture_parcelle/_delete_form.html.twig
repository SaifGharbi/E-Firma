<form method="post" action="{{ path('app_culture_parcelle_delete', {'id': culture_parcelle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ culture_parcelle.id) }}">
    <button class="btn btn-danger btn-sm mx-2">Supprimer</button>
</form>
