<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Livraison PDF</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
            line-height: 1.6;
        }
        h1 {
            text-align: center;
            color: #2E86C1;
        }
        .content {
            margin: 20px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 12px;
            color: gray;
        }
    </style>
</head>
<body>
<h1>Livraison Details</h1>
<div class="content">
    <p><strong>ID:</strong> {{ livraison.id }}</p>
    <p><strong>Address:</strong> {{ livraison.adresse }}</p>
    <p><strong>Delivery Date:</strong> {{ livraison.dateLivraison ? livraison.dateLivraison|date('Y-m-d') : 'Not Set' }}</p>
    <p><strong>Status:</strong> {{ livraison.statut ? 'Delivered' : 'Not Delivered' }}</p>
</div>
<div class="footer">
    <p>Generated on {{ "now"|date('Y-m-d H:i:s') }}</p>
</div>
</body>
</html>
